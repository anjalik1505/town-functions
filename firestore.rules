rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // If Firestore is only accessed from the Admin SDK in Cloud Functions, 
    // deny all direct client reads/writes:
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
